version: 2.1

defaults: &defaults
  docker:
    - image: cimg/node:13.8.0


jobs:
  failing_job:
    <<: *defaults
    steps:
      - checkout
      - run: return 1


workflows:
  reusability-workflow:
    jobs:
      - failing_job
      - rollback_job:
          when:
