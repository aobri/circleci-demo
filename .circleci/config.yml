version: 2.1

defaults: &defaults
  docker:
    - image: cimg/node:13.8.0


jobs:
  failing_job:
    <<: *defaults
    steps:
      - checkout
      - run: return 1
  rollback_job:
    <<: *defaults
    steps:
      - echo Hello Error!



workflows:
  failing-workflow:
    jobs:
      - failing_job
      - rollback_job:
          when: on_fail
